import React,{useEffect,useState}from'react';
import{ArrowRight,Menu,X}from'lucide-react';import Logo from'./Logo';
const links=[['Início','#top'],['Cognitia','#cognitia'],['Soluções','#solucoes'],['Projetos','#projetos'],['Contato','#contato']];
export default function SiteHeader(){const[scrolled,setScrolled]=useState(false),[open,setOpen]=useState(false),[active,setActive]=useState('#top');useEffect(()=>{const onScroll=()=>{setScrolled(window.scrollY>20);let current='#top';links.forEach(([,href])=>{const el=document.querySelector(href);if(el&&el.getBoundingClientRect().top<=150)current=href});setActive(current)};onScroll();window.addEventListener('scroll',onScroll,{passive:true});return()=>window.removeEventListener('scroll',onScroll)},[]);return <header className={`fixed inset-x-0 top-0 z-50 transition-all ${scrolled?'border-b border-white/10 bg-background/85 backdrop-blur-xl':'bg-transparent'}`}><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 md:h-20 md:px-8"><a href="#top" aria-label="Cognitia — início" onClick={()=>setOpen(false)}><Logo/></a><nav className="hidden items-center gap-1 lg:flex" aria-label="Navegação principal">{links.map(([label,href])=><a key={href} href={href} className={`rounded-lg px-4 py-2 text-sm font-medium transition ${active===href?'bg-white/5 text-white':'text-muted hover:text-white'}`}>{label}</a>)}</nav><a href="#contato" className="group hidden items-center gap-2 rounded-lg border border-primary/70 px-5 py-3 text-sm font-semibold transition hover:bg-primary/10 lg:flex">Fale conosco <ArrowRight className="h-4 w-4 transition group-hover:translate-x-1"/></a><button type="button" className="grid h-11 w-11 place-items-center rounded-full border border-white/10 bg-white/5 lg:hidden" aria-label={open?'Fechar menu':'Abrir menu'} aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X className="h-5 w-5"/>:<Menu className="h-5 w-5"/>}</button></div>{open&&<nav className="border-t border-white/10 bg-background/95 px-5 py-5 backdrop-blur-xl lg:hidden">{links.map(([label,href])=><a key={href} href={href} onClick={()=>setOpen(false)} className="block rounded-xl px-4 py-3 font-display text-lg hover:bg-white/5">{label}</a>)}</nav>}</header>}
